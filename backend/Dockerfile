# Финальная, самая надежная версия

# 1. Берем официальный легкий образ Python
FROM python:3.9-slim

# 2. Устанавливаем переменную окружения, чтобы логи Python не буферизировались
ENV PYTHONUNBUFFERED 1

# 3. Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# 4. Копируем файл с зависимостями
COPY requirements.txt .

# 5. Устанавливаем зависимости
RUN pip install --no-cache-dir -r requirements.txt

# 6. Копируем весь остальной код проекта в рабочую директорию /app
COPY . .